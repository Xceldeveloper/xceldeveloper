# Nuxt 4 / @clack need Node 20.12+ (styleText). Pin 22 to match package.json engines.
[build.environment]
  NODE_VERSION = "22"
  # Ensures allowIndexing is true for the production site (not deploy previews).
  # Netlify also sets CONTEXT=production automatically on production deploys.
